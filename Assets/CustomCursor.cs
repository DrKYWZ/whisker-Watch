using UnityEngine;

public class CustomCursor : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // Vector2 mousePos = Camera.main.ScreenToWorldPoint(Input.mousePosition); // Get the current mouse position in world coordinatest 
        // transform.position = new Vector2(Input.mousePosition.x, Input.mousePosition.y); // Get the current mouse position
    }    

    // Update is called once per frame
    void Update()
    {
        
    }
}
